# 91164-ds-Java-Revisão

# Interface
Se tiver mudanças na brach atual, faça commit.
git add .

git commit -m "salvar"

git push -u origin nome-da-branch

### Para Mudar de branch

git branch

git checkout nome-da-branch